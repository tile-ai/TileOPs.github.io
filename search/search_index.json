{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"TileOPs","text":"<p>High-performance LLM operator library built on TileLang.</p>"},{"location":"#installation","title":"Installation","text":"<pre><code>pip install tileops\n</code></pre>"},{"location":"#quick-start","title":"Quick Start","text":"<pre><code>import torch\nfrom tileops.ops import GemmOp\n\nop = GemmOp(dtype=torch.float16)\nC = op.forward(A, B)\n</code></pre>"},{"location":"#links","title":"Links","text":"<ul> <li>GitHub</li> <li>Contributing</li> <li>Development Guide</li> </ul>"},{"location":"api/attention/","title":"Attention Operators","text":""},{"location":"api/attention/#flash-attention","title":"Flash Attention","text":""},{"location":"api/attention/#multiheadattentionfwdop","title":"<code>MultiHeadAttentionFwdOp</code>","text":"<p>Multi-head attention forward pass using Flash Attention algorithm.</p> <ul> <li>Supported architectures: Ampere (SM 80/86), Hopper (SM 90)</li> <li>Supported dtypes: <code>float16</code>, <code>bfloat16</code></li> </ul> <pre><code>from tileops.ops import MultiHeadAttentionFwdOp\n\nop = MultiHeadAttentionFwdOp(dtype=torch.float16)\noutput = op.forward(Q, K, V)\n</code></pre>"},{"location":"api/attention/#multiheadattentionbwdop","title":"<code>MultiHeadAttentionBwdOp</code>","text":"<p>Multi-head attention backward pass.</p>"},{"location":"api/attention/#multiheadattentiondecodewithkvcacheop","title":"<code>MultiHeadAttentionDecodeWithKVCacheOp</code>","text":"<p>Optimized decode-phase attention with KV-cache support.</p>"},{"location":"api/attention/#multiheadattentiondecodepagedwithkvcacheop","title":"<code>MultiHeadAttentionDecodePagedWithKVCacheOp</code>","text":"<p>Decode attention with paged KV-cache for memory-efficient serving.</p>"},{"location":"api/attention/#deepseek-attention-variants","title":"DeepSeek Attention Variants","text":""},{"location":"api/attention/#multiheadlatentattentiondecodewithkvcacheop","title":"<code>MultiHeadLatentAttentionDecodeWithKVCacheOp</code>","text":"<p>DeepSeek MLA (Multi-head Latent Attention) decode.</p>"},{"location":"api/attention/#deepseeksparseattentiondecodewithkvcacheop","title":"<code>DeepSeekSparseAttentionDecodeWithKVCacheOp</code>","text":"<p>DeepSeek DSA (Dynamic Sparse Attention) decode.</p>"},{"location":"api/attention/#linear-attention","title":"Linear Attention","text":""},{"location":"api/attention/#engram","title":"Engram","text":"<ul> <li><code>EngramLinearAttentionFwdOp</code></li> <li><code>EngramLinearAttentionBwdOp</code></li> <li><code>EngramLinearAttentionDecodeOp</code></li> </ul>"},{"location":"api/attention/#gated-deltanet","title":"Gated DeltaNet","text":"<ul> <li><code>GatedDeltaNetFwdOp</code></li> <li><code>GatedDeltaNetBwdOp</code></li> <li><code>GatedDeltaNetDecodeOp</code></li> </ul>"},{"location":"api/elementwise/","title":"Elementwise Operators","text":""},{"location":"api/elementwise/#unary-operations-30","title":"Unary Operations (30+)","text":"<p>Math functions applied element-by-element:</p> Op Description <code>AbsOp</code> Absolute value <code>NegOp</code> Negation <code>ExpOp</code> / <code>Exp2Op</code> / <code>Expm1Op</code> Exponential variants <code>LogOp</code> / <code>Log2Op</code> / <code>Log10Op</code> / <code>Log1pOp</code> Logarithm variants <code>SqrtOp</code> / <code>RsqrtOp</code> Square root / reciprocal square root <code>SinOp</code> / <code>CosOp</code> / <code>TanOp</code> Trigonometric <code>SinhOp</code> / <code>CoshOp</code> / <code>TanhOp</code> Hyperbolic <code>AsinOp</code> / <code>AcosOp</code> / <code>AtanOp</code> Inverse trigonometric <code>SigmoidOp</code> Sigmoid activation <code>ReluOp</code> / <code>GeluOp</code> / <code>SiluOp</code> Activation functions <code>ClampOp</code> Value clamping <code>CeilOp</code> / <code>FloorOp</code> / <code>RoundOp</code> / <code>TruncOp</code> Rounding <code>BitwiseNotOp</code> Bitwise NOT <code>ReciprocalOp</code> 1/x <code>ErfOp</code> / <code>ErfcOp</code> / <code>ErfinvOp</code> Error function variants <code>SignOp</code> / <code>SgnOp</code> Sign functions <pre><code>from tileops.ops import GeluOp\n\nop = GeluOp(dtype=torch.float16)\noutput = op.forward(x)\n</code></pre>"},{"location":"api/elementwise/#fused-operations","title":"Fused Operations","text":""},{"location":"api/elementwise/#fusedgatedop","title":"<code>FusedGatedOp</code>","text":"<p>Fused gated activation: combines a binary operation with an elementwise activation in a single kernel launch (e.g., SwiGLU = <code>x * silu(gate)</code>).</p>"},{"location":"api/fft/","title":"FFT","text":""},{"location":"api/fft/#fftc2cop","title":"<code>FFTC2COp</code>","text":"<p>1D Complex-to-Complex FFT.</p>"},{"location":"api/fft/#fftc2clutop","title":"<code>FFTC2CLUTOp</code>","text":"<p>1D Complex-to-Complex FFT using lookup table optimization.</p>"},{"location":"api/linear-algebra/","title":"Linear Algebra Operators","text":""},{"location":"api/linear-algebra/#gemm","title":"GEMM","text":""},{"location":"api/linear-algebra/#gemmop","title":"<code>GemmOp</code>","text":"<p>General matrix multiplication with support for transpose modes.</p> <ul> <li>Supported architectures: Ampere (SM 80/86), Hopper (SM 90)</li> <li>Supported dtypes: <code>float16</code>, <code>bfloat16</code>, <code>float32</code></li> </ul> <pre><code>from tileops.ops import GemmOp\n\nop = GemmOp(dtype=torch.float16)\nC = op.forward(A, B)  # C = A @ B\n</code></pre> <p>Transpose modes: <code>NN</code>, <code>NT</code>, <code>TN</code>, <code>TT</code></p> <p>Also handles GEMV (matrix-vector) cases internally.</p>"},{"location":"api/mhc/","title":"MHC","text":"<p>Manifold-Constrained Hyper-Connection.</p>"},{"location":"api/mhc/#manifoldconstrainedhyperconnectionpreop","title":"<code>ManifoldConstrainedHyperConnectionPreOp</code>","text":"<p>Pre-processing for manifold-constrained hyper-connections.</p>"},{"location":"api/mhc/#manifoldconstrainedhyperconnectionpostop","title":"<code>ManifoldConstrainedHyperConnectionPostOp</code>","text":"<p>Post-processing for manifold-constrained hyper-connections.</p>"},{"location":"api/normalization/","title":"Normalization Operators","text":""},{"location":"api/normalization/#layer-normalization","title":"Layer Normalization","text":""},{"location":"api/normalization/#layernormop","title":"<code>LayerNormOp</code>","text":"<p>Standard layer normalization.</p> <pre><code>from tileops.ops import LayerNormOp\n\nop = LayerNormOp(dtype=torch.float16)\noutput = op.forward(x, weight, bias)\n</code></pre>"},{"location":"api/normalization/#rmsnormop","title":"<code>RmsNormOp</code>","text":"<p>Root mean square normalization (used in LLaMA, etc.).</p>"},{"location":"api/normalization/#fused-normalization","title":"Fused Normalization","text":""},{"location":"api/normalization/#fusedaddlayernormop","title":"<code>FusedAddLayerNormOp</code>","text":"<p>Fused residual add + layer normalization (avoids extra memory read/write).</p>"},{"location":"api/normalization/#fusedaddrmsnormop","title":"<code>FusedAddRmsNormOp</code>","text":"<p>Fused residual add + RMS normalization.</p>"},{"location":"api/quantization/","title":"Quantization","text":""},{"location":"api/quantization/#fp8quantop","title":"<code>Fp8QuantOp</code>","text":"<p>FP8 quantization for inference optimization.</p>"},{"location":"api/quantization/#fp8lightingindexerop","title":"<code>Fp8LightingIndexerOp</code>","text":"<p>FP8 indexing for Lightning-style quantization workflows.</p>"},{"location":"api/reduction/","title":"Reduction Operators","text":""},{"location":"api/reduction/#softmax","title":"Softmax","text":""},{"location":"api/reduction/#softmaxop","title":"<code>SoftmaxOp</code>","text":"<p>Numerically stable softmax.</p> <pre><code>from tileops.ops import SoftmaxOp\n\nop = SoftmaxOp(dtype=torch.float16)\noutput = op.forward(x, dim=-1)\n</code></pre>"},{"location":"api/reduction/#logsoftmaxop","title":"<code>LogSoftmaxOp</code>","text":"<p>Log-softmax for numerical stability in loss computation.</p>"},{"location":"api/reduction/#logsumexpop","title":"<code>LogSumExpOp</code>","text":"<p>Log-sum-exp reduction.</p>"},{"location":"api/reduction/#arg-reductions","title":"Arg Reductions","text":""},{"location":"api/reduction/#argmaxop-argminop","title":"<code>ArgmaxOp</code> / <code>ArgminOp</code>","text":"<p>Index of maximum/minimum value along a dimension.</p>"},{"location":"api/topk/","title":"Top-k","text":""},{"location":"api/topk/#topkselectorop","title":"<code>TopkSelectorOp</code>","text":"<p>Top-k selection operator, commonly used in MoE routing.</p>"},{"location":"benchmarks/","title":"Overall","text":"<p>Performance measurements for TileOPs operators across hardware and configurations.</p> <p>Note</p> <p>Benchmark results are continuously updated from nightly CI runs. Numbers reflect the latest validated results on our benchmark hardware.</p>"},{"location":"benchmarks/#regression-detection","title":"Regression Detection","text":"<ul> <li>Threshold: &gt;10% latency regression vs 7-day rolling median triggers alert</li> <li>Correctness: Headroom ratio &gt;0.8 triggers drift warning</li> <li>Test time: &gt;20% regression in benchmark duration flagged</li> </ul>"},{"location":"benchmarks/attention/","title":"Attention Benchmarks","text":"<p>Performance results for attention operators.</p> <p>Note</p> <p>This page will be populated with benchmark data from nightly CI runs.</p>"},{"location":"benchmarks/attention/#flash-attention-forward","title":"Flash Attention Forward","text":"Shape (B, H, S, D) Dtype TileOPs (ms) PyTorch (ms) Speedup TFLOPS \u2014 \u2014 \u2014 \u2014 \u2014 \u2014"},{"location":"benchmarks/attention/#flash-attention-decode","title":"Flash Attention Decode","text":"Shape (B, H, S, D) Dtype TileOPs (ms) Baseline (ms) Speedup Bandwidth (GB/s) \u2014 \u2014 \u2014 \u2014 \u2014 \u2014"},{"location":"benchmarks/attention/#gqa-forward","title":"GQA Forward","text":"Shape Dtype TileOPs (ms) PyTorch (ms) Speedup TFLOPS \u2014 \u2014 \u2014 \u2014 \u2014 \u2014"},{"location":"benchmarks/elementwise/","title":"Elementwise Benchmarks","text":"<p>Note</p> <p>This page will be populated with benchmark data from nightly CI runs.</p>"},{"location":"benchmarks/elementwise/#unary-operations","title":"Unary Operations","text":"Op Shape Dtype TileOPs (ms) PyTorch (ms) Speedup Bandwidth (GB/s) \u2014 \u2014 \u2014 \u2014 \u2014 \u2014 \u2014"},{"location":"benchmarks/elementwise/#binary-operations","title":"Binary Operations","text":"Op Shape Dtype TileOPs (ms) PyTorch (ms) Speedup Bandwidth (GB/s) \u2014 \u2014 \u2014 \u2014 \u2014 \u2014 \u2014"},{"location":"benchmarks/elementwise/#fused-gated-activations","title":"Fused Gated Activations","text":"Activation Shape Dtype TileOPs (ms) Separate Ops (ms) Speedup \u2014 \u2014 \u2014 \u2014 \u2014 \u2014"},{"location":"benchmarks/gemm/","title":"GEMM Benchmarks","text":"<p>Performance results for matrix multiplication operators.</p> <p>Note</p> <p>This page will be populated with benchmark data from nightly CI runs.</p>"},{"location":"benchmarks/gemm/#dense-gemm","title":"Dense GEMM","text":"Shape (M, N, K) Dtype TileOPs (ms) cuBLAS (ms) Speedup TFLOPS \u2014 \u2014 \u2014 \u2014 \u2014 \u2014"},{"location":"benchmarks/gemm/#gemv","title":"GEMV","text":"Shape (M, N) Dtype TileOPs (ms) PyTorch (ms) Speedup Bandwidth (GB/s) \u2014 \u2014 \u2014 \u2014 \u2014 \u2014"},{"location":"benchmarks/gemm/#grouped-gemm","title":"Grouped GEMM","text":"Groups \u00d7 Shape Dtype TileOPs (ms) PyTorch (ms) Speedup TFLOPS \u2014 \u2014 \u2014 \u2014 \u2014 \u2014"},{"location":"benchmarks/normalization/","title":"Normalization Benchmarks","text":"<p>Note</p> <p>This page will be populated with benchmark data from nightly CI runs.</p>"},{"location":"benchmarks/normalization/#layernorm","title":"LayerNorm","text":"Shape (B, S, D) Dtype TileOPs (ms) PyTorch (ms) Speedup Bandwidth (GB/s) \u2014 \u2014 \u2014 \u2014 \u2014 \u2014"},{"location":"benchmarks/normalization/#rmsnorm","title":"RmsNorm","text":"Shape (B, S, D) Dtype TileOPs (ms) PyTorch (ms) Speedup Bandwidth (GB/s) \u2014 \u2014 \u2014 \u2014 \u2014 \u2014"},{"location":"benchmarks/normalization/#fused-add-layernorm","title":"Fused Add + LayerNorm","text":"Shape Dtype TileOPs (ms) Separate Ops (ms) Speedup Bandwidth (GB/s) \u2014 \u2014 \u2014 \u2014 \u2014 \u2014"},{"location":"blog/","title":"Blog","text":"<p>Technical deep-dives, release notes, and engineering insights from the TileOPs team.</p> <p>Posts coming soon.</p>"},{"location":"design/elementwise-ops/","title":"Elementwise Operator Family","text":""},{"location":"design/elementwise-ops/#1-overview","title":"1. Overview","text":"<p>TileOPs elementwise operator family covers 72 ops across 12 sub-categories, with fp8 support, torch.compile integration, kernel caching, and performance tuning.</p>"},{"location":"design/elementwise-ops/#scope-72-ops","title":"Scope (72 ops)","text":"Category Ops Count unary_math exp, log, sqrt, rsqrt, abs, neg, reciprocal, sign, sin, cos, floor, ceil, round, trunc, erf, log1p, expm1 17 binary_arith add, sub, mul, div, remainder, pow, floor_divide, lerp, maximum, minimum 10 activation relu, gelu, silu, sigmoid, tanh, leaky_relu, elu, selu, hardswish, hardsigmoid, hardtanh, softplus, mish, prelu 14 fused_gated_activation silu_and_mul (SwiGLU), gelu_and_mul, gelu_tanh_and_mul 3 comparison eq, ne, gt, lt, ge, le 6 bitwise bitwise_and, bitwise_or, bitwise_xor, bitwise_not 4 logical logical_not, logical_and, logical_or 3 special_elementwise where, clamp, masked_fill, nan_to_num, isnan, isinf, isfinite 7 rope neox, non_neox, llama31, yarn, longrope 5 dropout dropout 1 alibi ALiBi position encoding 1 sinusoidal Sinusoidal position encoding 1 72"},{"location":"design/elementwise-ops/#supported-dtypes","title":"Supported Dtypes","text":"<p>fp32, bf16, fp16, fp8_e4m3fn, fp8_e5m2</p>"},{"location":"design/elementwise-ops/#2-design-decisions","title":"2. Design Decisions","text":""},{"location":"design/elementwise-ops/#21-kernel-template-strategy","title":"2.1 Kernel Template Strategy","text":"<p>Ops are grouped by input signature into template base classes. Each concrete op is a 3-5 line subclass overriding only <code>op_func</code>. Ops with user-configurable parameters or non-standard signatures are implemented independently.</p> <p>Template to Op Mapping:</p> Template Signature Ops Count UnaryKernel <code>op_func(x) \u2192 y</code> unary_math (17); activation (9): relu, gelu, silu, sigmoid, tanh, hardswish, hardsigmoid, mish, selu; logical_not (1); bitwise_not (1); isnan, isinf, isfinite (3) 31 BinaryKernel <code>op_func(a, b) \u2192 y</code>, with <code>output_dtype</code> and broadcast binary_arith (10); comparison (6); logical_and, logical_or (2); bitwise_and, bitwise_or, bitwise_xor (3) 21 FusedGatedKernel <code>activation_func(gate) * value</code>, input <code>(M, 2N)</code> split silu_and_mul, gelu_and_mul, gelu_tanh_and_mul 3 Independent Custom signatures, no shared template base class leaky_relu, elu, hardtanh, softplus, prelu, where, clamp, masked_fill, nan_to_num, alibi, sinusoidal 11 Total 66 <p>rope (5 ops) and dropout (1 op) are in separate files (<code>rope.py</code>, <code>dropout.py</code>) due to fundamentally different kernel patterns (complex rotary addressing, PRNG state management).</p> <p>Notes:</p> <ul> <li>hardsigmoid\u3001selu have fixed constants (not user-configurable), hardcoded in <code>op_func</code>, classified under UnaryKernel.</li> <li>softplus has user-configurable beta/threshold \u2192 independent implementation.</li> <li>Comparison ops output <code>int8</code> at the kernel level; the Op layer casts to <code>torch.bool</code>.</li> </ul>"},{"location":"design/elementwise-ops/#22-file-organization","title":"2.2 File Organization","text":"<pre><code>tileops/kernels/\n\u251c\u2500\u2500 elementwise.py          # All template base classes + concrete kernel subclasses + independent kernels (~3,000 lines)\n\u251c\u2500\u2500 rope.py                 # 5 rope variants\n\u251c\u2500\u2500 dropout.py              # Dropout with PRNG\n\ntileops/ops/\n\u251c\u2500\u2500 elementwise.py          # All template base Op classes + concrete op subclasses + custom_op registration (~1,800 lines)\n\ntests/\n\u251c\u2500\u2500 ops/test_special_elementwise.py\n\u251c\u2500\u2500 test_elementwise_fp8.py\n\u251c\u2500\u2500 test_elementwise_compile.py\n\u251c\u2500\u2500 test_elementwise_independent_fp8.py\n\u251c\u2500\u2500 test_elementwise_strategy_bench.py\n\u251c\u2500\u2500 test_elementwise_caching_autotune.py\n\nbenchmarks/ops/\n\u251c\u2500\u2500 bench_unary_elementwise.py\n\u251c\u2500\u2500 bench_binary_elementwise.py\n\u251c\u2500\u2500 bench_independent_elementwise.py\n\u251c\u2500\u2500 bench_elementwise_fp8.py\n</code></pre> <p>Single <code>elementwise.py</code> for both kernels and ops. Tests split by concern (correctness, fp8, compile, strategy, caching). Benchmarks split by kernel template.</p>"},{"location":"design/elementwise-ops/#23-re-export-strategy","title":"2.3 Re-export Strategy","text":"<ul> <li><code>tileops/kernels/__init__.py</code>: Re-exports only 3 base classes (<code>UnaryKernel</code>, <code>BinaryKernel</code>, <code>FusedGatedKernel</code>).</li> <li><code>tileops/ops/__init__.py</code>: Re-exports only 3 base Op classes (<code>UnaryOp</code>, <code>BinaryOp</code>, <code>FusedGatedOp</code>).</li> <li>Concrete classes imported directly: <code>from tileops.kernels.elementwise import ReluKernel</code>.</li> </ul>"},{"location":"design/elementwise-ops/#24-concrete-kernel-class-per-op","title":"2.4 Concrete Kernel Class per Op","text":"<p>Each op is an explicit class:</p> <pre><code>class ReluKernel(UnaryKernel):\n    @staticmethod\n    def op_func(x):\n        return T.max(x, 0)\n</code></pre> <p>Chosen over factory functions for: IDE navigation, <code>isinstance</code> checks, traceback clarity, and per-op <code>default_config</code> / <code>autotune_configs</code> overrides.</p>"},{"location":"design/elementwise-ops/#25-torchcompile-support-custom_op-registration","title":"2.5 torch.compile Support: custom_op Registration","text":"<p>All 66 ops support <code>torch.compile</code> via <code>@torch.library.custom_op</code> registration.</p> <p>Registry mechanism: <code>_OP_REGISTRY = weakref.WeakValueDictionary()</code> keyed by <code>id(instance)</code>. Each Op instance registers itself at construction time; the custom_op wrapper looks up the instance by key and delegates to <code>_eager_forward</code>.</p> <p>Factory functions (7 total, registered at module load time):</p> Factory Ops Signature <code>_register_unary_custom_op</code> 31 unary ops <code>(x, instance_key) \u2192 Tensor</code> <code>_register_binary_custom_op</code> 21 binary ops <code>(a, b, out_shape, instance_key) \u2192 Tensor</code> <code>_register_fused_gated_custom_op</code> 3 fused gated ops <code>(x, M, N, instance_key) \u2192 Tensor</code> <code>_register_prelu_custom_op</code> prelu <code>(x, weight, instance_key) \u2192 Tensor</code> <code>_register_where_custom_op</code> where <code>(cond, x, y, instance_key) \u2192 Tensor</code> <code>_register_masked_fill_custom_op</code> masked_fill <code>(x, mask, instance_key) \u2192 Tensor</code> <code>_register_generative_custom_op</code> alibi, sinusoidal <code>(device_carrier, num_a, num_b, instance_key) \u2192 Tensor</code> <p>Each Op class's <code>forward</code> dispatches through <code>_wrapped(...)</code> when torch.compile is active, falling back to <code>_eager_forward</code> otherwise.</p>"},{"location":"design/elementwise-ops/#3-kernel-architecture","title":"3. Kernel Architecture","text":""},{"location":"design/elementwise-ops/#31-shape-convention","title":"3.1 Shape Convention","text":"<p>Unary ops: Arbitrary shape, flattened to 1D <code>(N_total,)</code> by the Op layer before passing to kernel.</p> <p>Binary ops: Stride-based offset with N-dim broadcast (equivalent to PyTorch TensorIterator semantics).</p> <p>Op layer processing: 1. <code>torch.broadcast_shapes(a.shape, b.shape)</code> \u2192 output shape 2. Align dimensions (left-pad with 1), compute broadcast strides (stride=0 for broadcast dims) 3. Dimension coalescing (<code>coalesce_broadcast_dims</code>): merge adjacent dims with compatible broadcast patterns to minimize divmod count 4. Both inputs <code>.contiguous().view(-1)</code> flattened to 1D; <code>(coalesced_shape, a_strides, b_strides)</code> passed as compile-time constants 5. Kernel computes output via flat index (contiguous), maps to input offsets through divmod chain</p> <p>FusedGated ops: Input <code>(M, 2N)</code> \u2192 2D grid, no broadcast.</p>"},{"location":"design/elementwise-ops/#32-memory-access-pattern-no-shared-memory","title":"3.2 Memory Access Pattern: No Shared Memory","text":"<p>Elementwise ops are pure memory-bandwidth bound (arithmetic intensity \u2248 0). The optimal path is:</p> <pre><code>Global Memory \u2500\u2500direct read\u2500\u2500\u25b6 Register \u2500\u2500Compute\u2500\u2500\u25b6 Register \u2500\u2500direct write\u2500\u2500\u25b6 Global Memory\n</code></pre> <ul> <li>No shared memory allocation or budget constraints</li> <li><code>T.Parallel</code> maps threads + 128-bit vectorized coalesced access (<code>uint4</code> loads/stores)</li> <li><code>LegalizeSafeMemoryAccess</code> compiler pass auto-inserts boundary guards</li> </ul>"},{"location":"design/elementwise-ops/#33-three-kernel-strategies","title":"3.3 Three Kernel Strategies","text":""},{"location":"design/elementwise-ops/#strategy-1-direct-compiler-optimized","title":"Strategy 1: <code>direct</code> \u2014 Compiler-optimized","text":"<p>Each thread processes 1 element. TileLang may auto-vectorize. Least code, but compiler behavior not fully controllable.</p> <pre><code>def _make_unary_direct(N, dtype, op_func, output_dtype=None, threads=256):\n    @T.prim_func\n    def main(x: T.Tensor((N,), dtype), y: T.Tensor((N,), out_dtype)):\n        with T.Kernel(T.ceildiv(N, threads), threads=threads) as bx:\n            for i in T.Parallel(threads):\n                y[bx * threads + i] = op_func(x[bx * threads + i])\n    return main\n</code></pre>"},{"location":"design/elementwise-ops/#strategy-2-explicit_parallel-explicit-vectorization-control","title":"Strategy 2: <code>explicit_parallel</code> \u2014 Explicit vectorization control","text":"<p>Each thread processes <code>num_per_thread</code> elements. <code>T.Parallel(threads, num_per_thread)</code> explicitly guides the compiler for vectorized access.</p> <pre><code>def _make_unary_explicit(N, dtype, op_func, output_dtype=None, threads=256, num_per_thread=8):\n    block_size = threads * num_per_thread\n    @T.prim_func\n    def main(x: T.Tensor((N,), dtype), y: T.Tensor((N,), out_dtype)):\n        with T.Kernel(T.ceildiv(N, block_size), threads=threads) as bx:\n            for i, j in T.Parallel(threads, num_per_thread):\n                idx = (bx * threads + i) * num_per_thread + j\n                y[idx] = op_func(x[idx])\n    return main\n</code></pre>"},{"location":"design/elementwise-ops/#strategy-3-register_copy-explicit-register-staging","title":"Strategy 3: <code>register_copy</code> \u2014 Explicit register staging","text":"<p>Explicit <code>T.alloc_fragment</code> + <code>T.copy</code> for global\u2194register transfers. <code>T.copy</code> generates vectorized load/store instructions.</p> <pre><code>def _make_unary_regcopy(N, dtype, op_func, output_dtype=None, threads=256, num_per_thread=8):\n    block_size = threads * num_per_thread\n    @T.prim_func\n    def main(x: T.Tensor((N,), dtype), y: T.Tensor((N,), out_dtype)):\n        with T.Kernel(T.ceildiv(N, block_size), threads=threads) as bx:\n            x_reg = T.alloc_fragment((block_size,), dtype)\n            y_reg = T.alloc_fragment((block_size,), out_dtype)\n            T.copy(x[bx * block_size:(bx + 1) * block_size], x_reg)\n            for i, j in T.Parallel(threads, num_per_thread):\n                y_reg[i * num_per_thread + j] = op_func(x_reg[i * num_per_thread + j])\n            T.copy(y_reg, y[bx * block_size:(bx + 1) * block_size])\n    return main\n</code></pre>"},{"location":"design/elementwise-ops/#binary-kernel-stride-based-offset","title":"Binary Kernel: Stride-Based Offset","text":"<p>Binary ops use 1D output flat index + stride-based offset for N-dim broadcast. All shape/stride values are compile-time constants; the divmod chain is unrolled at compilation.</p> <p>All three strategies (direct, explicit_parallel, register_copy) are supported. When <code>register_copy</code> is requested on broadcast inputs (ndim &gt; 1), it silently downgrades to <code>explicit_parallel</code> because <code>T.copy</code> requires contiguous slices.</p>"},{"location":"design/elementwise-ops/#strategy-defaults-h200-benchmarks","title":"Strategy Defaults (H200 benchmarks)","text":"Kernel Type Default Strategy Rationale UnaryKernel <code>register_copy</code> H200: register_copy wins for fp16/bf16 across all tested shapes BinaryKernel <code>explicit_parallel</code> Broadcast inputs incompatible with register_copy; explicit_parallel is most general FusedGatedKernel <code>explicit_parallel</code> H200: ~2\u00d7 faster than direct (e.g. silu_and_mul: 3.04 TB/s vs 1.50 TB/s) fp8 (all types) <code>explicit_parallel</code> register_copy unreliable for 8-bit fragments"},{"location":"design/elementwise-ops/#config-defaults","title":"Config Defaults","text":"<p>Strategy-aware <code>num_per_thread</code> defaults (H200 benchmarks):</p> Strategy fp32 fp16/bf16 fp8 <code>explicit_parallel</code> 4 4 16 <code>register_copy</code> 4 8 \u2014 <code>direct</code> \u2014 \u2014 \u2014 <p>Rationale: - fp32 \u00d7 4B \u00d7 4 = 16B = 128-bit <code>uint4</code> alignment - fp16/bf16 under <code>explicit_parallel</code>: npt=4 yields 42% bandwidth gain over npt=8 - fp16/bf16 under <code>register_copy</code>: npt=8 \u00d7 2B = 16B = 128-bit vectorized loads - fp8: npt=16 \u00d7 1B = 16B = 128-bit alignment</p> <p><code>threads</code> fixed at 256 for all strategies. Elementwise ops have low register pressure; 256 threads achieves good occupancy on SM80+.</p>"},{"location":"design/elementwise-ops/#34-template-base-classes","title":"3.4 Template Base Classes","text":""},{"location":"design/elementwise-ops/#unarykernel","title":"UnaryKernel","text":"<pre><code>class UnaryKernel(Kernel):\n    supported_archs = [80, 86, 89, 90]\n    STRATEGIES = [\"direct\", \"explicit_parallel\", \"register_copy\"]\n    DEFAULT_STRATEGY = \"register_copy\"\n    OUTPUT_DTYPE = None        # subclass override (e.g. torch.bool for predicates)\n    SUPPORTED_DTYPES = None    # subclass override to restrict input dtypes\n\n    @staticmethod\n    def op_func(x):\n        raise NotImplementedError\n\n    def __init__(self, N_total, dtype, strategy=None, config=None, tune=False):\n        # dtype validation\n        # fp8: override strategy to explicit_parallel\n        # _build_kernel \u2192 _make_unary_{direct,explicit,regcopy}\n        # init_config \u2192 cache _compiled_fn\n\n    def forward(self, x):\n        return self._compiled_fn(x)\n</code></pre> <p>Key implementation details: - <code>_build_kernel</code> dispatches to the appropriate <code>_make_unary_*</code> factory based on strategy - <code>init_config</code> caches <code>_compiled_fn</code> by pre-applying config params (threads, npt) to the kernel builder - fp8 handling: <code>_wrap_fp8_accumulation</code> wraps <code>op_func</code> with cast-in/cast-out; <code>_fp8_output_dtype</code> signals the Op layer to apply post-cast - Autotune override catches serialization failures (op_func closures aren't pickleable) and falls back to default_config</p>"},{"location":"design/elementwise-ops/#binarykernel","title":"BinaryKernel","text":"<pre><code>class BinaryKernel(Kernel):\n    supported_archs = [80, 86, 89, 90]\n    STRATEGIES = [\"direct\", \"explicit_parallel\", \"register_copy\"]\n    DEFAULT_STRATEGY = \"explicit_parallel\"\n    OUTPUT_DTYPE = None        # \"int8\" for comparison/logical ops\n    SUPPORTED_DTYPES = None\n\n    @staticmethod\n    def op_func(a, b):\n        raise NotImplementedError\n\n    def __init__(self, N_total, dtype, coalesced_shape, a_strides, b_strides,\n                 a_numel, b_numel, strategy=None, config=None, tune=False):\n        # dtype validation\n        # register_copy auto-downgrade on broadcast inputs\n        # _build_kernel \u2192 _make_binary_{direct,explicit,register_copy}\n        # init_config \u2192 cache _compiled_fn\n\n    def forward(self, a, b):\n        return self._compiled_fn(a, b)\n</code></pre> <ul> <li>Comparison ops use <code>OUTPUT_DTYPE = \"int8\"</code> at kernel level; Op layer (<code>_BoolOutputBinaryOp</code>) casts to <code>torch.bool</code></li> <li><code>register_copy</code> silently downgrades to <code>explicit_parallel</code> when any input has broadcast dimensions (ndim &gt; 1)</li> </ul>"},{"location":"design/elementwise-ops/#fusedgatedkernel","title":"FusedGatedKernel","text":"<pre><code>class FusedGatedKernel(Kernel):\n    supported_archs = [80, 86, 89, 90]\n    STRATEGIES = [\"direct\", \"explicit_parallel\"]\n    DEFAULT_STRATEGY = \"explicit_parallel\"\n    SUPPORTED_DTYPES = None\n\n    @staticmethod\n    def activation_func(x):\n        raise NotImplementedError\n\n    def __init__(self, M, N, dtype, strategy=None, config=None, tune=False):\n        # _build_kernel \u2192 _make_fused_gated_{direct,explicit}\n        # init_config \u2192 cache _compiled_fn\n\n    def forward(self, x):\n        return self._compiled_fn(x)\n</code></pre> <ul> <li>Input: single <code>(M, 2N)</code> tensor (gate + value packed); output: <code>(M, N)</code></li> <li>No <code>register_copy</code> \u2014 FusedGated has a 2D grid pattern incompatible with 1D fragment copy</li> <li>Single tensor interface matches vLLM/FlashInfer/xformers convention</li> </ul>"},{"location":"design/elementwise-ops/#independent-ops-11-ops","title":"Independent Ops (11 ops)","text":"<p>Each has its own builder function, <code>__init__</code>, and <code>forward</code>. Shared patterns: - <code>_make_*_kernel</code> with <code>is_fp8</code> branching: fp8 uses explicit_parallel with fp16 accumulation; non-fp8 uses register_copy - <code>_compiled_fn</code> caching in <code>init_config</code> - <code>_fp8_output_dtype</code> for Op-layer post-cast</p> Op Extra Parameters Builder leaky_relu negative_slope (scalar) <code>_make_leaky_relu_kernel</code> elu alpha (scalar) <code>_make_elu_kernel</code> hardtanh min_val, max_val (scalars) <code>_make_hardtanh_kernel</code> softplus beta, threshold (scalars) <code>_make_softplus_kernel</code> prelu weight (tensor) <code>_make_prelu_kernel</code> where \u2014 <code>_make_where_kernel</code> clamp min_val, max_val (scalars) <code>_make_clamp_kernel</code> masked_fill fill_value (scalar) <code>_make_masked_fill_kernel</code> nan_to_num nan, posinf, neginf (scalars) <code>_make_nan_to_num_kernel</code> alibi \u2014 <code>_make_alibi_kernel</code> sinusoidal \u2014 <code>_make_sinusoidal_kernel</code>"},{"location":"design/elementwise-ops/#4-fp8-support","title":"4. fp8 Support","text":""},{"location":"design/elementwise-ops/#41-accumulation-strategy","title":"4.1 Accumulation Strategy","text":"<p>fp8 dtypes lack precision for intermediate computation. All fp8 kernels use fp16 accumulation:</p> Input dtype Accumulation Output Post-cast (Op layer) e4m3fn fp16 e4m3fn (saturating <code>T.Cast</code>) None \u2014 kernel output is already e4m3fn e5m2 fp16 fp16 (no cast in kernel) Non-saturating <code>.to(e5m2)</code> preserving Inf/NaN <p>The distinction: e4m3fn has no Inf representation, so saturating cast is correct. e5m2 supports Inf/NaN, so the final cast must be non-saturating (PyTorch <code>.to()</code> semantics).</p>"},{"location":"design/elementwise-ops/#42-implementation","title":"4.2 Implementation","text":"<p>Shared helper <code>_wrap_fp8_accumulation(base_op, dtype, dtype_str, arity)</code>: - Wraps any <code>op_func</code> with fp8 cast-in/cast-out logic - Applied uniformly in <code>_get_effective_op_func</code> for all three template kernels - Non-fp8 dtypes pass through unchanged</p> <p>Scalar clamping <code>_clamp_to_dtype_range(value, dtype)</code>: - Prevents TVM FloatImm range-check failures for scalar literals exceeding fp8 range (e.g., <code>fill_value=1e4</code> into e4m3fn whose max is 448) - NaN passed through; Inf mapped to dtype max/min</p> <p>Config for fp8: - <code>npt=16</code> (1B \u00d7 16 = 128-bit alignment) - Strategy forced to <code>explicit_parallel</code> (register_copy unreliable for 8-bit fragments) - Autotune grid: <code>threads_opts=[128,256,512]</code>, <code>npt_opts=[16,32]</code></p>"},{"location":"design/elementwise-ops/#5-op-layer-architecture","title":"5. Op Layer Architecture","text":""},{"location":"design/elementwise-ops/#51-broadcast-utility-coalesce_broadcast_dims","title":"5.1 Broadcast Utility: <code>coalesce_broadcast_dims</code>","text":"<p>The core utility for binary ops. Converts arbitrary N-dim broadcast into minimal <code>(coalesced_shape, a_strides, b_strides)</code>.</p> <p>Algorithm: 1. <code>torch.broadcast_shapes</code> \u2192 output shape 2. Right-align, left-pad with 1 3. Compute C-contiguous strides (stride=0 for broadcast dims) 4. Merge adjacent dims where all operands have compatible stride patterns 5. Remove trivial size-1 groups</p> <p>Coalescing effect:</p> Scenario shapes Coalesced ndim Divmod count same-shape <code>(B,S,D) + (B,S,D)</code> 1 0 bias add <code>(B,S,D) + (1,1,D)</code> 2 1 scaling <code>(B,S,D) + (B,S,1)</code> 2 1 attn mask <code>(B,H,S,S) + (1,1,S,S)</code> 2 1 interleaved <code>(B,H,S,S) + (B,1,1,S)</code> 3 2 outer product <code>(M,1) + (1,N)</code> 2 1"},{"location":"design/elementwise-ops/#52-template-op-classes","title":"5.2 Template Op Classes","text":"<p>UnaryOp: <pre><code>class UnaryOp(Op):\n    def forward(self, x):\n        orig_shape = x.shape\n        x = x.contiguous().reshape(-1)     # flatten to 1D\n        y = self.kernel(x)                  # kernel handles boundary\n        y = y.reshape(orig_shape)\n        return _apply_fp8_post_cast(y, self.kernel)  # e5m2 non-saturating cast\n</code></pre></p> <p>BinaryOp: <pre><code>class BinaryOp(Op):\n    def __init__(self, a_shape, b_shape, dtype, ...):\n        out_shape, coalesced_shape, a_strides, b_strides = coalesce_broadcast_dims(a_shape, b_shape)\n        self.kernel = self.kernel_cls(N_total, dtype, coalesced_shape, a_strides, b_strides, ...)\n\n    def forward(self, a, b):\n        a_flat = a.contiguous().view(-1)\n        b_flat = b.contiguous().view(-1)\n        y = self.kernel(a_flat, b_flat)\n        return y.reshape(self.out_shape)\n</code></pre></p> <p>FusedGatedOp: Input <code>(M, 2N)</code> \u2192 kernel \u2192 output <code>(M, N)</code>. No reshape needed.</p> <p>All Op classes include validation: CUDA device check, dtype matching, numel matching.</p>"},{"location":"design/elementwise-ops/#53-torchcompile-dispatch","title":"5.3 torch.compile Dispatch","text":"<p>Each Op's <code>forward</code> checks for <code>_wrapped</code>: - If <code>_wrapped</code> is set: dispatch through custom_op (supports torch.compile tracing) - Otherwise: call <code>_eager_forward</code> directly</p> <p>Instance-based registry (<code>_OP_REGISTRY</code>) enables the custom_op wrapper to look up the correct Op instance at runtime.</p>"},{"location":"design/elementwise-ops/#6-kernel-caching","title":"6. Kernel Caching","text":"<p>Each kernel instance pre-compiles its kernel function in <code>init_config</code>:</p> <pre><code>def init_config(self, config=None, tune=False):\n    super().init_config(config, tune)\n    cfg = self.config\n    self._compiled_fn = self.kernel(cfg[\"threads\"], cfg[\"num_per_thread\"])\n</code></pre> <p>This avoids JIT lookup overhead on every <code>forward()</code> call. The compiled function is cached per-instance, not globally.</p> <p>Autotune serialization: <code>op_func</code>/<code>activation_func</code> closures aren't pickleable. The autotune override catches serialization failures and falls back to <code>default_config</code>.</p>"},{"location":"design/hardware-dispatch/","title":"Hardware-Aware Dispatch","text":""},{"location":"design/hardware-dispatch/#context","title":"Context","text":"<p>Different GPU architectures offer distinct instruction sets and performance characteristics:</p> Architecture SM Version Key Features Hopper SM 90 WGMMA, TMA, warp specialization, FP8 <p>A single kernel implementation cannot optimally serve all architectures.</p>"},{"location":"design/hardware-dispatch/#decision","title":"Decision","text":"<p>Each Op declares a <code>default_kernel_map</code> that maps SM architecture identifiers to Kernel classes:</p> <pre><code>class MultiHeadAttentionFwdOp(Op):\n    @property\n    def default_kernel_map(self):\n        return {\n            \"hopper\": FlashAttnV3FwdKernel,\n        }\n</code></pre> <p>At runtime, <code>dispatch_kernel()</code> queries the GPU's SM version via <code>get_sm_version()</code> and selects the matching kernel.</p>"},{"location":"design/hardware-dispatch/#sm-version-detection","title":"SM Version Detection","text":"<pre><code># tileops/utils/\ndef get_sm_version() -&gt; int:\n    \"\"\"Returns SM version of the current CUDA device (e.g., 80, 86, 90).\"\"\"\n</code></pre>"},{"location":"design/hardware-dispatch/#consequences","title":"Consequences","text":"<ul> <li>Ops automatically use the best kernel for the current hardware</li> <li>New architectures can be added by extending the kernel map</li> <li>Users can override the kernel map for custom dispatch logic</li> <li>Fallback behavior when no architecture-specific kernel exists</li> </ul>"},{"location":"design/two-layer-architecture/","title":"Two-Layer Architecture","text":""},{"location":"design/two-layer-architecture/#context","title":"Context","text":"<p>TileOPs needs to support multiple GPU architectures (Ampere, Hopper) with architecture-specific optimizations while presenting a clean, stable API to users.</p>"},{"location":"design/two-layer-architecture/#decision","title":"Decision","text":"<p>We separate the system into two layers:</p>"},{"location":"design/two-layer-architecture/#l1-kernel-layer","title":"L1 \u2014 Kernel Layer","text":"<ul> <li>Written in TileLang \u2014 GPU-native kernel implementations</li> <li>Hardware-specific \u2014 Separate implementations per SM architecture when beneficial</li> <li>Configuration-driven \u2014 Block sizes, pipeline stages, and other tuning parameters are externalized</li> <li>Autotunable \u2014 Each kernel declares <code>autotune_configs</code> for performance search</li> </ul> <pre><code>tileops/kernels/\n\u251c\u2500\u2500 flash_attn/          # Flash Attention v2 (Ampere), v3 (Hopper)\n\u251c\u2500\u2500 flash_decode/        # Paged KV-cache decode kernels\n\u251c\u2500\u2500 gemm/                # Matrix multiplication\n\u251c\u2500\u2500 norm/                # LayerNorm, RmsNorm, BatchNorm, etc.\n\u251c\u2500\u2500 reduction/           # Softmax, reduce, cumulative, argreduce\n\u251c\u2500\u2500 elementwise.py       # Unary + binary elementwise ops\n\u2514\u2500\u2500 kernel.py            # Kernel base class\n</code></pre>"},{"location":"design/two-layer-architecture/#l2-op-layer","title":"L2 \u2014 Op Layer","text":"<ul> <li>Pure Python \u2014 Stateless dispatchers</li> <li>Framework-compatible \u2014 Works with CUDA-Graph and <code>torch.compile</code></li> <li>Dtype-validated \u2014 Explicit input/output dtype contracts</li> <li>Architecture-aware \u2014 Queries SM version at runtime, selects appropriate kernel</li> </ul> <pre><code>tileops/ops/\n\u251c\u2500\u2500 attention/           # MHA, GQA, MLA, NSA, Linear Attention\n\u251c\u2500\u2500 gemm/                # GEMM, Grouped GEMM\n\u251c\u2500\u2500 norm/                # All normalization ops\n\u251c\u2500\u2500 reduction/           # Softmax, reduce, cumulative\n\u251c\u2500\u2500 elementwise/         # Unary, binary, fused\n\u2514\u2500\u2500 op.py                # Op base class\n</code></pre>"},{"location":"design/two-layer-architecture/#dispatch-flow","title":"Dispatch Flow","text":"<pre><code>User calls Op.forward(inputs)\n  \u2192 Op validates dtypes\n  \u2192 Op.dispatch_kernel() selects kernel by SM version\n  \u2192 Kernel.forward() launches TileLang program on GPU\n  \u2192 Results returned to user\n</code></pre>"},{"location":"design/two-layer-architecture/#consequences","title":"Consequences","text":"<p>Benefits: - Clean separation of concerns \u2014 hardware complexity stays in L1 - Multiple kernel implementations per op (e.g., Flash Attention v2 vs v3) - User code is architecture-agnostic - Autotuning at kernel level without touching op interface</p> <p>Trade-offs: - Two files to create per operator (kernel + op) - Kernel map maintenance when adding new architectures</p>"}]}